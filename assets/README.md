This folder contains static assets used by the UI.

- lab.jpg — background image for the application. Replace the placeholder with your own lab photo.

How to replace:
1) Put your image at this path as `lab.jpg` (JPEG recommended, ~200–400KB ideal).
2) No server restart is needed; the browser will load `/assets/lab.jpg`.
